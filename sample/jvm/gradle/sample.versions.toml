# Dependencies that only this sample needs.
#
# Kotlin, katachi and kotest are NOT here on purpose: they come from the root catalog
# (`../../gradle/libs.versions.toml`, read as `libs`), which is their single source of
# truth for every sample.

[versions]
ktor = "3.6.0"
logback = "1.6.3"

[libraries]
ktorServerCore = { module = "io.ktor:ktor-server-core", version.ref = "ktor" }
ktorServerNetty = { module = "io.ktor:ktor-server-netty", version.ref = "ktor" }
ktorServerContentNegotiation = { module = "io.ktor:ktor-server-content-negotiation", version.ref = "ktor" }
ktorSerializationKotlinxJson = { module = "io.ktor:ktor-serialization-kotlinx-json", version.ref = "ktor" }
ktorServerTestHost = { module = "io.ktor:ktor-server-test-host", version.ref = "ktor" }
# HOCON (`application.conf`) needs no extra artifact: ktor-server-core already depends on
# com.typesafe:config. There is no `ktor-server-config-hocon` in Ktor 3.6.0.
logbackClassic = { module = "ch.qos.logback:logback-classic", version.ref = "logback" }
