# Reuse outputs produced by other successful builds.
org.gradle.caching=true
# Reuse the build configuration between runs.
org.gradle.configuration-cache=true
# Enough heap/metaspace for the Kotlin compiler and the kotest runner (D15).
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1g
